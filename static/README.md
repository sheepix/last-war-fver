# Static assets

Put static files here (e.g. images). `scripts/build.mjs` copies this folder into `dist/` during build.

Recommended Markdown image usage:

```md
![](/static/images/your-image.png)
```

Use an absolute path (starting with `/`) so it works from any article URL.
