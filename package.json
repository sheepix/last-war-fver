{
  "name": "lw-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run -s build && wrangler pages dev dist --port 8788 --live-reload",
    "deploy": "./deploy-pages.sh"
  },
  "dependencies": {
    "marked": "^15.0.7"
  }
}
